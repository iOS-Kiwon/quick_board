import 'package:flutter/material.dart';

abstract final class YachtColors {
  static const Color background = Color(0xFF0A0A0A);       // Pure black
  static const Color card = Color(0xFF141010);             // Dark red-tinted card
  static const Color primary = Color(0xFFCC2222);          // Main red
  static const Color primaryBright = Color(0xFFFF4444);    // Bright red (headings)
  static const Color text = Color(0xFFEEE0E0);             // Warm white
  static const Color textDim = Color(0xFF8A7070);          // Dim text
  static const Color border = Color(0xFF2A1818);           // Dark border
  static const Color scorePositive = Color(0xFF44AA66);    // Green (positive score)
  static const Color bonus = Color(0xFFFFAA22);            // Amber (bonus row emphasis)
}
